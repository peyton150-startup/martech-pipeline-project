// The test harness validates against the exact same compiled schemas the app
// ships and the collect endpoint enforces — one source of truth, not a copy.
export { validators, getValidator } from "../../lib/tracking/validators";
